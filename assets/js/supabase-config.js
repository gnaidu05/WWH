/* Kalam backend configuration.
 *
 * Paste your Supabase project's values below. Both are safe to commit — the
 * anon key is a PUBLIC client key (Row Level Security protects your data).
 *
 * Find them in: Supabase Dashboard → Project Settings → API
 *   - Project URL      → url
 *   - Project API keys → anon / public  → anonKey
 *
 * Until these are filled in, the site runs in demo mode (sample data only,
 * and Join/Sign in explain that the backend isn't connected yet).
 */
window.KALAM_SUPABASE = {
  url: "https://rcwgrajsoomabcyebihe.supabase.co",
  anonKey: "sb_publishable_Brg2AcprxFazw5mt0RJOcA_dkcSpOwk"
};
